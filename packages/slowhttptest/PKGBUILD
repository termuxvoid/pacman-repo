# Maintainer: Alienkrishn [Anon4You]
# Ported from TermuxVoid APT repo (packages/slowhttptest)

pkgname="slowhttptest"
pkgver=1.9.0
pkgrel=1
pkgdesc='Application Layer Denial of Service attack simulator'
arch=('any')
url="https://github.com/shekyan/slowhttptest"
depends=('git' 'openssl' 'build-essential' 'findutils')
install="slowhttptest.install"
options=('!strip' '!debug' '!emptydirs')

build() {
    return 0
}

package() {
    return 0
}
