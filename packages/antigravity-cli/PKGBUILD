# Maintainer: Alienkrishn [Anon4You]
# Ported from TermuxVoid APT repo (packages/antigravity-cli)

pkgname="antigravity-cli"
pkgver=1.2.7
pkgrel=0
pkgdesc='AI‑powered coding assistant (glibc wrapper for Termux)'
arch=('any')
url="https://antigravity.google/"
depends=('curl' 'clang' 'python' 'glibc')
install="antigravity-cli.install"
options=('!strip' '!debug' '!emptydirs')

build() {
    return 0
}

package() {
    return 0
}
