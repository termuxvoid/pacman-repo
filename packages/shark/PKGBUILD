# Maintainer: Alienkrishn [Anon4You]
# Ported from TermuxVoid APT repo (packages/shark)

pkgname="shark"
pkgver=2.5.1.1
pkgrel=0
pkgdesc='Future of Phishing with less delay'
arch=('any')
url="https://github.com/E343IO/shark"
depends=('git' 'curl' 'wget' 'php' 'unzip' '7zip' 'ncurses-utils' 'proot' 'resolv-conf' 'sox' 'ffmpeg' 'dialog')
install="shark.install"
options=('!strip')

build() {
    return 0
}

package() {
    return 0
}
