# Maintainer: Alienkrishn [Anon4You]
# Ported from TermuxVoid APT repo (packages/hashid)

pkgname="hashid"
pkgver=3.1.4
pkgrel=1
pkgdesc='Advanced hash identifier'
arch=('any')
url="https://github.com/psypanda/hashID"
depends=('python' 'python-pip')
install="hashid.install"
options=('!strip' '!debug' '!emptydirs')

build() {
    return 0
}

package() {
    return 0
}
