# Maintainer: Alienkrishn [Anon4You]
# Ported from TermuxVoid APT repo (packages/void-fonts)

pkgname="void-fonts"
pkgver=1.0
pkgrel=0
pkgdesc='Custom font collection for Termux'
arch=('any')
url="https://github.com/termuxvoid/Void-Fonts.git"
depends=('git' 'bash')
install="void-fonts.install"
options=('!strip')

build() {
    return 0
}

package() {
    return 0
}
