# Maintainer: Alienkrishn [Anon4You]
# Ported from TermuxVoid APT repo (packages/codex-cli)

pkgname="codex-cli"
pkgver=0.150.1
pkgrel=1
pkgdesc="Codex CLI for Termux (npm version)"
arch=('any')
url="https://github.com/DioNanos/codex-termux"
depends=('npm')
install="codex-cli.install"
options=('!strip' '!debug' '!emptydirs')

build() {
    return 0
}

package() {
    return 0
}
