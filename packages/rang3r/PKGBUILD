# Maintainer: Alienkrishn [Anon4You]
# Ported from TermuxVoid APT repo (packages/rang3r)

pkgname="rang3r"
pkgver=1.0
pkgrel=1
pkgdesc='Multi-threaded IP + port scanner'
arch=('any')
url="https://github.com/floriankunushevci/rang3r"
depends=('git' 'python2')
install="rang3r.install"
options=('!strip' '!debug' '!emptydirs')

build() {
    return 0
}

package() {
    return 0
}
