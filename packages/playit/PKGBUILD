# Maintainer: Alienkrishn [Anon4You]
# Ported from TermuxVoid APT repo (packages/playit)

pkgname="playit"
pkgver=0.17.1
pkgrel=1
pkgdesc='Global Proxy for Server Hosting Without Port Forwarding'
arch=('any')
url="https://playit.gg/"
depends=('git' 'rust' 'build-essential' 'pkg-config')
install="playit.install"
options=('!strip' '!debug' '!emptydirs')

build() {
    return 0
}

package() {
    return 0
}
