# Maintainer: Alienkrishn [Anon4You]
# Ported from TermuxVoid APT repo (packages/interlace)

pkgname="interlace"
pkgver=1.9.8
pkgrel=0
pkgdesc='Turn single-threaded CLI tools into fast multi-threaded engines'
arch=('any')
url="https://github.com/codingo/Interlace"
depends=('git' 'python')
install="interlace.install"
options=('!strip' '!debug' '!emptydirs')

build() {
    return 0
}

package() {
    return 0
}
