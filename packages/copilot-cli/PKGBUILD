# Maintainer: Alienkrishn [Anon4You]
# Ported from TermuxVoid APT repo (packages/copilot-cli)

pkgname="copilot-cli"
pkgver=1.0.83
pkgrel=0
pkgdesc='GitHub Copilot CLI (glibc version)'
arch=('any')
url="https://github.com/github/copilot-cli"
depends=('git' 'gh' 'glibc' 'clang' 'python' 'curl' 'tar')
install="copilot-cli.install"
options=('!strip' '!debug' '!emptydirs')

build() {
    return 0
}

package() {
    return 0
}
