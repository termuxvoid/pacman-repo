# Maintainer: Alienkrishn [Anon4You]
# Ported from TermuxVoid APT repo (packages/haiti)

pkgname="haiti"
pkgver=4.0.0
pkgrel=1
pkgdesc='Hash type identifier (CLI tool)'
arch=('any')
url="https://github.com/noraj/haiti"
depends=('ruby')
install="haiti.install"
options=('!strip' '!debug' '!emptydirs')

build() {
    return 0
}

package() {
    return 0
}
