#!/bin/bash

post_install() {

set -e

echo "Installing user-scanner..."
pip install user-scanner==1.5.0
echo "user-scanner installed successfully."
return 0
}

post_remove() {

pip uninstall -y user-scanner 2>/dev/null && echo "user-scanner removed." || echo "user-scanner not found."
return 0
}

