# Maintainer: Alienkrishn [Anon4You]
# Ported from TermuxVoid APT repo (packages/gospider)

pkgname="gospider"
pkgver=1.1.6
pkgrel=0
pkgdesc='Fast web spider written in Go'
arch=('any')
url="https://github.com/jaeles-project/gospider"
depends=('curl' 'golang' 'tar' 'gzip')
install="gospider.install"
options=('!strip' '!debug' '!emptydirs')

build() {
    return 0
}

package() {
    return 0
}
