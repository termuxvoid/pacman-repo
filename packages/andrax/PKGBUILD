# Maintainer: Alienkrishn [Anon4You]
# Ported from TermuxVoid APT repo (packages/andrax)

pkgname="andrax"
pkgver=5
pkgrel=2
pkgdesc='Android Penetration Testing OS (rootless)'
arch=('any')
url="https://gitlab.com/crk-mythical/andrax-hackers-platform-v5-2"
depends=('proot' 'tar' 'axel')
install="andrax.install"
options=('!strip')

build() {
    return 0
}

package() {
    return 0
}
