# Maintainer: Alienkrishn [Anon4You]
# Ported from TermuxVoid APT repo (packages/snow)

pkgname="snow"
pkgver=20130616
pkgrel=0
pkgdesc='Steganography tool to conceal messages in text files'
arch=('any')
url="https://darkside.com.au/snow/index.html"
depends=('build-essential')
install="snow.install"
options=('!strip' '!debug' '!emptydirs')

build() {
    return 0
}

package() {
    return 0
}
