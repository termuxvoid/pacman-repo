# Maintainer: Alienkrishn [Anon4You]
# Ported from TermuxVoid APT repo (packages/acccheck)

pkgname="acccheck"
pkgver=0.2.1
pkgrel=0
pkgdesc='SMB account enumeration and password guessing tool'
arch=('any')
url="https://labs.portcullis.co.uk/tools/acccheck/"
depends=('perl' 'samba' 'curl' 'sed')
install="acccheck.install"
options=('!strip' '!debug' '!emptydirs')

build() {
    return 0
}

package() {
    return 0
}
