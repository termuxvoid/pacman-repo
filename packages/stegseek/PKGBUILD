# Maintainer: Alienkrishn [Anon4You]
# Ported from TermuxVoid APT repo (packages/stegseek)

pkgname="stegseek"
pkgver=0.6
pkgrel=0
pkgdesc='Lightning-fast steghide password cracker'
arch=('any')
url="https://github.com/RickdeJager/StegSeek"
depends=('curl' 'tar' 'gzip' 'make' 'cmake' 'clang' 'binutils' 'libmhash' 'libmcrypt' 'libjpeg-turbo' 'zlib')
install="stegseek.install"
options=('!strip' '!debug' '!emptydirs')

build() {
    return 0
}

package() {
    return 0
}
