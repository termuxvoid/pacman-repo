# Maintainer: Alienkrishn [Anon4You]
# Ported from TermuxVoid APT repo (packages/nosqli)

pkgname="nosqli"
pkgver=0.5.4
pkgrel=1
pkgdesc='NoSQL injection detection and exploitation tool'
arch=('any')
url="https://github.com/Charlie-belmer/nosqli"
depends=('git' 'golang')
install="nosqli.install"
options=('!strip' '!debug' '!emptydirs')

build() {
    return 0
}

package() {
    return 0
}
