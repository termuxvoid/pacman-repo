# Maintainer: Alienkrishn [Anon4You]
# Ported from TermuxVoid APT repo (packages/tte)

pkgname="tte"
pkgver=0.15.0
pkgrel=0
pkgdesc='Terminal Text Effects and Animation Library'
arch=('any')
url="https://pypi.org/project/terminaltexteffects/"
depends=('python' 'python-pip')
install="tte.install"
options=('!strip')

build() {
    return 0
}

package() {
    return 0
}
