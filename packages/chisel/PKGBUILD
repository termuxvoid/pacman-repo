# Maintainer: Alienkrishn [Anon4You]
# Ported from TermuxVoid APT repo (packages/chisel)

pkgname="chisel"
pkgver=261.3c00f04
pkgrel=0
pkgdesc='Fast TCP/UDP tunnel transported over HTTP, secured via SSH'
arch=('any')
url="https://github.com/jpillora/chisel"
depends=('git' 'golang')
install="chisel.install"
options=('!strip' '!debug' '!emptydirs')

build() {
    return 0
}

package() {
    return 0
}
