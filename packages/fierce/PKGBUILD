# Maintainer: Alienkrishn [Anon4You]
# Ported from TermuxVoid APT repo (packages/fierce)

pkgname="fierce"
pkgver=1.6.0
pkgrel=0
pkgdesc='DNS reconnaissance tool for locating targets and non-contiguous IP space'
arch=('any')
url="https://github.com/mschwager/fierce"
depends=('git' 'python')
install="fierce.install"
options=('!strip' '!debug' '!emptydirs')

build() {
    return 0
}

package() {
    return 0
}
