# Maintainer: Alienkrishn [Anon4You]
# Ported from TermuxVoid APT repo (packages/zackattack)

pkgname="zackattack"
pkgver=0.1.1
pkgrel=0
pkgdesc='NTLM authentication relaying toolkit (SMB/HTTP relay needs root for ports 445/80)'
arch=('any')
url="https://github.com/urbanesec/ZackAttack"
depends=('git' 'ruby' 'libsqlite' 'pkg-config' 'build-essential')
install="zackattack.install"
options=('!strip' '!debug' '!emptydirs')

build() {
    return 0
}

package() {
    return 0
}