# Maintainer: Alienkrishn [Anon4You]
# Ported from TermuxVoid APT repo (packages/dorks-eye)

pkgname="dorks-eye"
pkgver=2.0
pkgrel=1
pkgdesc='Google Hacking Dork Scraping and Searching Tool'
arch=('any')
url="https://github.com/BullsEye0/dorks-eye"
depends=('git' 'python' 'python-pip' 'pip-basic-depends' 'python-cryptography' 'libffi' 'libiconv')
install="dorks-eye.install"
options=('!strip' '!debug' '!emptydirs')

build() {
    return 0
}

package() {
    return 0
}
