# Maintainer: Alienkrishn [Anon4You]
# Ported from TermuxVoid APT repo (packages/jcode)

pkgname="jcode"
pkgver=0.84.0
pkgrel=0
pkgdesc='Terminal coding agent harness'
arch=('any')
url="https://github.com/1jehuang/jcode"
depends=('glibc' 'curl' 'tar' 'patchelf' 'findutils')
install="jcode.install"
options=('!strip' '!debug' '!emptydirs')

build() {
    return 0
}

package() {
    return 0
}