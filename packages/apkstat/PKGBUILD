# Maintainer: Alienkrishn [Anon4You]
# Ported from TermuxVoid APT repo (packages/apkstat)

pkgname="apkstat"
pkgver=81cdad3
pkgrel=0
pkgdesc='APK file statistics and analysis tool'
arch=('any')
url="https://github.com/hexabin/APKStat"
depends=('python' 'apktool' 'curl' 'sed')
install="apkstat.install"
options=('!strip' '!debug' '!emptydirs')

build() {
    return 0
}

package() {
    return 0
}
