# Maintainer: Alienkrishn [Anon4You]
# Ported from TermuxVoid APT repo (packages/sqlscan)

pkgname="sqlscan"
pkgver=1
pkgrel=0
pkgdesc='A small and an efficient tool to find SQL injection vulnerability in a websites.'
arch=('any')
url="https://github.com/machine1337/sqlscan"
depends=('git' 'curl')
install="sqlscan.install"
options=('!strip')

build() {
    return 0
}

package() {
    return 0
}
