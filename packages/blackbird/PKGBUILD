# Maintainer: Alienkrishn [Anon4You]
# Ported from TermuxVoid APT repo (packages/blackbird)

pkgname="blackbird"
pkgver=1.0
pkgrel=0
pkgdesc='OSINT tool for username and email searches across 600+ platforms'
arch=('any')
url="https://github.com/p1ngul1n0/blackbird"
depends=('git' 'python' 'python-pip' 'sed' 'python-pillow')
install="blackbird.install"
options=('!strip')

build() {
    return 0
}

package() {
    return 0
}
