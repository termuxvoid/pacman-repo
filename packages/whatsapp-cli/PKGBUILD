# Maintainer: Alienkrishn [Anon4You]
# Ported from TermuxVoid APT repo (packages/whatsapp-cli)

pkgname="whatsapp-cli"
pkgver=1.3.3
pkgrel=1
pkgdesc='Standalone CLI for WhatsApp using multidevice protocol'
arch=('any')
url="https://github.com/vicentereig/whatsapp-cli"
depends=('git' 'golang')
install="whatsapp-cli.install"
options=('!strip' '!debug' '!emptydirs')

build() {
    return 0
}

package() {
    return 0
}
