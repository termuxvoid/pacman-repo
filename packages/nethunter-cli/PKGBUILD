# Maintainer: Alienkrishn [Anon4You]
# Ported from TermuxVoid APT repo (packages/nethunter-cli)

pkgname="nethunter-cli"
pkgver=2025.4
pkgrel=1
pkgdesc='Kali NetHunter Rootless Edition CLI'
arch=('any')
url="https://www.kali.org/"
depends=('proot' 'tar' 'axel')
install="nethunter-cli.install"
options=('!strip' '!debug' '!emptydirs')

build() {
    return 0
}

package() {
    return 0
}
