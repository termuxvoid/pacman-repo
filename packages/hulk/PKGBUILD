# Maintainer: Alienkrishn [Anon4You]
# Ported from TermuxVoid APT repo (packages/hulk)

pkgname="hulk"
pkgver=1.0.1
pkgrel=0
pkgdesc='Web server DoS testing tool (Golang port)'
arch=('any')
url="https://github.com/grafov/hulk"
depends=('git' 'golang')
install="hulk.install"
options=('!strip' '!debug' '!emptydirs')

build() {
    return 0
}

package() {
    return 0
}
