# Maintainer: Alienkrishn [Anon4You]
# Ported from TermuxVoid APT repo (packages/utim-cli)

pkgname="utim-cli"
pkgver=2.3.19
pkgrel=1
pkgdesc="UTIM CLI — AI-powered unified terminal intelligence manager"
arch=('any')
url="https://github.com/emend-ai/utim"
depends=('python' 'npm')
install="utim-cli.install"
options=('!strip' '!debug' '!emptydirs')

build() {
    return 0
}

package() {
    return 0
}
