# Maintainer: Alienkrishn [Anon4You]
# Ported from TermuxVoid APT repo (packages/hermes-agent)

pkgname="hermes-agent"
pkgver=0.19.0
pkgrel=1
pkgdesc='AI-powered coding assistant and workflow automation tool'
arch=('any')
url="https://github.com/NousResearch/hermes-agent"
depends=('npm' 'uv' 'git' 'python' 'clang' 'rust' 'make' 'pkg-config' 'libffi' 'openssl' 'nodejs' 'ripgrep' 'ffmpeg' 'python-psutil' 'python-pillow' 'python-cryptography')
install="hermes-agent.install"
options=('!strip' '!debug' '!emptydirs')

build() {
    return 0
}

package() {
    return 0
}
