# Maintainer: Alienkrishn [Anon4You]
# Ported from TermuxVoid APT repo (packages/maigret)

pkgname="maigret"
pkgver=0.6.4
pkgrel=0
pkgdesc='OSINT username search tool with 3000+ sites'
arch=('any')
url="https://maigret.readthedocs.io"
depends=('python' 'python-lxml' 'python-pillow' 'python-psutil' 'rust' 'make' 'pkg-config' 'cmake')
install="maigret.install"
options=('!strip')

build() {
    return 0
}

package() {
    return 0
}
