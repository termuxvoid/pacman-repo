# Maintainer: Alienkrishn [Anon4You]
# Ported from TermuxVoid APT repo (packages/bad-pdf)

pkgname="bad-pdf"
pkgver=1.1
pkgrel=1
pkgdesc="Create malicious PDFs to steal NTLM hashes from Windows"
arch=('any')
url="https://github.com/deepzec/Bad-Pdf"
depends=('curl' 'python' 'termux-tools')
install="bad-pdf.install"
options=('!strip' '!debug' '!emptydirs')

build() {
    return 0
}

package() {
    return 0
}