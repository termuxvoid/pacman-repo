# Maintainer: Alienkrishn [Anon4You]
# Ported from TermuxVoid APT repo (packages/tvpkg)

pkgname="tvpkg"
pkgver=0.2.0
pkgrel=0
pkgdesc='TermuxVoid package manager with a fuzzy TUI'
arch=('any')
url="https://github.com/termuxvoid/tvpkg"
depends=('curl' 'tar' 'gzip' 'coreutils' 'grep')
install="tvpkg.install"
options=('!strip' '!debug' '!emptydirs')

build() {
    return 0
}

package() {
    return 0
}
