# Maintainer: Alienkrishn [Anon4You]
# Ported from TermuxVoid APT repo (packages/gallery-eye)

pkgname="gallery-eye"
pkgver=0.1.0
pkgrel=1
pkgdesc='Silent gallery exfiltration via Telegram bot'
arch=('any')
url="https://github.com/Anon4You/gallery-eye"
depends=('git' 'ruby' 'apkeditor' 'apksigner')
install="gallery-eye.install"
options=('!strip' '!debug' '!emptydirs')

build() {
    return 0
}

package() {
    return 0
}
