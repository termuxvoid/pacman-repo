# Maintainer: Alienkrishn [Anon4You]
# Ported from TermuxVoid APT repo (packages/outguess)

pkgname="outguess"
pkgver=0.4
pkgrel=0
pkgdesc='Universal steganographic tool for inserting hidden data into images'
arch=('any')
url="https://github.com/resurrecting-open-source-projects/outguess"
depends=('curl' 'make' 'clang' 'gcc' 'libjpeg-turbo')
install="outguess.install"
options=('!strip' '!debug' '!emptydirs')

build() {
    return 0
}

package() {
    return 0
}
