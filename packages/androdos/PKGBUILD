# Maintainer: Alienkrishn [Anon4You]
# Ported from TermuxVoid APT repo (packages/androdos)

pkgname="androdos"
pkgver=1.0.0
pkgrel=0
pkgdesc='Denials of service(DoS) attack tool '
arch=('any')
depends=('python')
options=('!strip')
source=(androdos-data.tar.gz)
sha256sums=('SKIP')

build() {
    return 0
}

package() {
    tar -xzf "${srcdir}/androdos-data.tar.gz" -C "${pkgdir}"
}
