# Maintainer: Alienkrishn [Anon4You]
# Ported from TermuxVoid APT repo (packages/trufflehog)

pkgname="trufflehog"
pkgver=3.97.1
pkgrel=0
pkgdesc='Find leaked credentials and secrets in code and data'
arch=('any')
url="https://github.com/trufflesecurity/trufflehog"
depends=('git' 'golang')
install="trufflehog.install"
options=('!strip')

build() {
    return 0
}

package() {
    return 0
}
