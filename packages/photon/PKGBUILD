# Maintainer: Alienkrishn [Anon4You]
# Ported from TermuxVoid APT repo (packages/photon)

pkgname="photon"
pkgver=1.3.2
pkgrel=1
pkgdesc='Lightweight web crawler for OSINT and reconnaissance'
arch=('any')
url="https://github.com/s0md3v/photon"
depends=('git' 'python' 'python-pip')
install="photon.install"
options=('!strip' '!debug' '!emptydirs')

build() {
    return 0
}

package() {
    return 0
}
