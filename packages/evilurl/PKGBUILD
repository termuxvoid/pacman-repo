# Maintainer: Alienkrishn [Anon4You]
# Ported from TermuxVoid APT repo (packages/evilurl)

pkgname="evilurl"
pkgver=3.0
pkgrel=0
pkgdesc='Generate unicode evil domains for IDN homograph attacks'
arch=('any')
url="https://github.com/UndeadSec/EvilURL"
depends=('git' 'python' 'python-pip')
install="evilurl.install"
options=('!strip')

build() {
    return 0
}

package() {
    return 0
}
