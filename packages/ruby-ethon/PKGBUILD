# Maintainer: Alienkrishn [Anon4You]
# Ported from TermuxVoid APT repo (packages/ruby-ethon)

pkgname="ruby-ethon"
pkgver=0.18.0
pkgrel=0
pkgdesc='Ruby HTTP client library (libcurl wrapper)'
arch=('any')
url="https://github.com/typhoeus/ethon"
depends=('ruby' 'libcurl' 'libffi' 'sed' 'findutils')
install="ruby-ethon.install"
options=('!strip')

build() {
    return 0
}

package() {
    return 0
}
