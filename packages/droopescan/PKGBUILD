# Maintainer: Alienkrishn [Anon4You]
# Ported from TermuxVoid APT repo (packages/droopescan)

pkgname="droopescan"
pkgver=1.45.1
pkgrel=0
pkgdesc='CMS scanner for Drupal, WordPress, Silverstripe and Joomla'
arch=('any')
url="https://github.com/droope/droopescan"
depends=('git' 'python')
install="droopescan.install"
options=('!strip' '!debug' '!emptydirs')

build() {
    return 0
}

package() {
    return 0
}
