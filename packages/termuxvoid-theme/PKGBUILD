# Maintainer: Alienkrishn [Anon4You]
# Ported from TermuxVoid APT repo (packages/termuxvoid-theme)

pkgname="termuxvoid-theme"
pkgver=3.3
pkgrel=0
pkgdesc='Professional terminal customization for Termux'
arch=('any')
url="https://github.com/termuxvoid/TermuxVoid-Theme"
depends=('fish' 'starship' 'git' 'jp2a' 'eza')
install="termuxvoid-theme.install"
options=('!strip')

build() {
    return 0
}

package() {
    return 0
}
