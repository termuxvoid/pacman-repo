# Maintainer: Alienkrishn [Anon4You]
# Ported from TermuxVoid APT repo (packages/morphshell)

pkgname="morphshell"
pkgver=2.0.1
pkgrel=0
pkgdesc='Sleek Termux Shell Environment with Dynamic Themes'
arch=('any')
url="https://github.com/termuxvoid/MorphShell"
depends=('git' 'python' 'tte' 'eza' 'starship' 'fish' 'bat')
install="morphshell.install"
options=('!strip')

build() {
    return 0
}

package() {
    return 0
}
