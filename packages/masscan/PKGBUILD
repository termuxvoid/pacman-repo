# Maintainer: Alienkrishn [Anon4You]
# Ported from TermuxVoid APT repo (packages/masscan)

pkgname="masscan"
pkgver=1.3.2
pkgrel=0
pkgdesc='High-speed TCP port scanner'
arch=('any')
url="https://github.com/robertdavidgraham/masscan"
depends=('git' 'make' 'clang' 'libpcap' 'sed')
install="masscan.install"
options=('!strip')

build() {
    return 0
}

package() {
    return 0
}
