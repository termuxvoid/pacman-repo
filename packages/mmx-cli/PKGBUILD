# Maintainer: Alienkrishn [Anon4You]
# Ported from TermuxVoid APT repo (packages/mmx-cli)

pkgname="mmx-cli"
pkgver=1.0.22
pkgrel=1
pkgdesc='CLI tool for MiniMax AI'
arch=('any')
url="https://github.com/MiniMax-AI/cli"
depends=('npm')
install="mmx-cli.install"
options=('!strip' '!debug' '!emptydirs')

build() {
    return 0
}

package() {
    return 0
}
